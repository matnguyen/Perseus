[pytest]

minversion = 7.0

# Where tests live
testpaths =
    tests

# Show useful output
addopts =
    -ra
    -q

markers =
    pipeline: marks tests as part of the Perseus pipeline test suite
    dev: marks tests as development-only tests