# Deprecated code

This directory contains legacy and experimental implementations that are **no longer used in Perseus**.

---

## Contents

### Loss functions (`deprecated/losses`)

This subdirectory contains earlier loss functions explored during development.

---

### Model architectures (`deprecated/models`)

This subdirectory contains earlier neural network architectures evaluated during development. 